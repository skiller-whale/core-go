package main

func main() {
	// Print two UNIX times:
	//
	// 1. the "epochalypse", which is the maximum UNIX time representable by an `int32` type (`int64(int32((2 << 30) - 1))`)
	// 2. the time that a 32-bit UNIX clock will show one second after the epochalypse.
}
