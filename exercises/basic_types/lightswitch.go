package main

func main() {
	// declare a boolean variable called upstairsSwitch
	// declare a boolean variable called downstairsSwitch
	// declare a boolean variable called hallLightOn which takes `upstairsSwitch` and `downstairsSwitch` as its inputs, and should change whenever one of the switch variables changes

	if hallLightOn {
		println("The light is on.")
	} else {
		println("The light is off.")
	}
}
